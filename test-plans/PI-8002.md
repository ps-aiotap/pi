# Test plan — PI-8002

## Scope and references

- Fix spec: `pi/specs/PI-8002.md`

## Code hotspots (seed from repo)

- Dashboard report book: `dashboard/src/app/asset-vantage/report-book/report-book.component.ts`
- PAR / performance Lambda aggregation: `av_v3_lambda/src/PerformanceApiReport/par_response.py` (e.g. `period_quantity_added` / `period_quantity_sold` mapped into position `period_response` around 653–950+)
- Benchmark UI: `dashboard/src/app/asset-vantage/dashboard/widget/benchmark/benchmark.component.ts`

## Environment / data prerequisites

- HC UAT tenant from spec (if matched) or reporter environment.
- Entity/account/security identifiers from the PI description.

## Verify the fix

| # | Case | Steps | Expected |
|---|------|-------|----------|
| 1 | Happy path from PI | Execute reporter scenario end-to-end | Symptom gone; values match expected business rule |
| 2 | Boundary dates | Run adjacent period / as-of dates | No incorrect carry-forward of quantities or balances |

## Regression (each row anchored)

| Area | Code / UX anchor | Check |
|------|------------------|-----|
| Report Book load | `dashboard/src/app/asset-vantage/report-book/report-book.component.ts` | Open report book; add/remove widget; PDF/export if applicable |
| Benchmark table | `dashboard/src/app/asset-vantage/dashboard/widget/benchmark/benchmark.component.ts` | MTD/QTD/YTD columns populate consistently |
| Dashboard shell | `dashboard/src/app/test/component/dashboard.component.spec.ts` | Unit suite passes |

## Adjacent

- Flows sharing the same code path as the confirmed fix.

## Automation mapping

| Layer | Framework / location | Exists | Gap |
|-------|---------------------|--------|-----|
| Dashboard | Karma + Jasmine (`dashboard/karma.conf.js`), glob `dashboard/src/**/*.spec.ts` | `dashboard/src/app/asset-vantage/report-book/report-book.component.spec.ts`; widget specs under `dashboard/src/app/test/` | Add targeted spec once fix file is known |
| PHP | PHPUnit in `controller/config/php/composer.json` | `controller/unittest/tests/` (e.g. `DateCalculationHelperTest.php` uses PHPUnit `TestCase`) | Extend with tests colocated with changed PHP modules |
| Lambda | `av_v3_lambda/` | Check CI / package tests | Add tests if harness exists |

**Manual-only:** Integrated reporting and custodian feeds often need real data; document manual cases until fixtures exist.
