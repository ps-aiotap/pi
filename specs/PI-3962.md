# Balance sheet - Cost of sold out equities can be seen on balance sheet.

**ItemId:** `PI-3962`

## Metadata

| Field | Value |
|------|--------|
| Group | On Hold/Reopened - Dev Team |
| Status | Feedback |
| Priority | High |
| Environment | — |
| Reporter | Vaishakh Sudhir, John Bezgam, Madhuram Joshi, Kevin Pagliaro |
| TEAM (CSV) | SCOT |
| Module | — |
| Client Name | — |

## Client environment URL

### Match type
No catalog match: No **Client Name** match; narrative did not yield a unique catalog tenant.

### HC UAT URL
*Not in `pi/input/urls/` — confirm with reporter or extend catalog.*

## Suggested assignment

| | |
|--|--|
| **Team** | SCOT Misc |
| **Leader** | Anil Chandran |
| **Rationale** | CSV **TEAM** `SCOT` maps to roster **SCOT Misc**. |
| **CSV assignees** | CSV **Assigned To**: Rushikesh Ghorpade; **Developer**: Shital Thorat. |

## Summary

Balance sheet - Cost of sold out equities can be seen on balance sheet.

## Reproduction / symptoms

1. Use **HC UAT** URL when a tenant match exists; otherwise use reporter-provided environment.
2. Follow steps implied in **Bug Description** (entity/account IDs in ticket where given).
3. Capture report/widget names, filters, and dates from the ticket.

## Root cause (hypothesis + evidence)

- Triage required: confirm data vs. presentation vs. feed/provider scope per ticket.
- Repo touchpoints depend on area: PAR/report-book often involves Lambda `par_response` pipelines and dashboard report-book/benchmark widgets; ingestion/sync issues touch `controller/` feed and transaction sync paths; GL/master-data issues touch Spartans GL areas.

**Examples (not exhaustive):**
- PAR quantity / open-close: `av_v3_lambda/src/PerformanceApiReport/par_response.py` (e.g. `period_quantity_added` / `period_quantity_sold` mapped into position `period_response` around 653–950+)
- Report Book UI: `dashboard/src/app/asset-vantage/report-book/report-book.component.ts`
- Benchmark widget: `dashboard/src/app/asset-vantage/dashboard/widget/benchmark/benchmark.component.ts`; catalog: `dashboard/src/app/asset-vantage/models/widgets.ts` (`BenchmarkComponent`, `cumulativeMultiperiodReport`)

## Proposed fix (behavior-level)

1. Reproduce under controlled entity/data; narrow to API vs UI vs upstream feed.
2. Implement minimal change matching existing patterns (config/env for endpoints; no hardcoded tenant secrets).
3. Add regression coverage per `pi/test-plans/PI-3962.md` after approval.

## Impact / blast radius

Depends on final root cause: may affect reporting periods, widgets, feeds, or master data. Treat production reporting and sync flows as high blast radius until scoped.

## Acceptance criteria

- Symptom no longer reproduces on **HC UAT** (or agreed environment) with ticket’s scenario.
- No regression on adjacent report periods / widgets called out in test plan.
- Any config or feature flag documented for rollout.

## Open questions

- Tenant confirmation if URL was ambiguous or missing.
- Whether provider/third-party (e.g. custodian, Morningstar) owns part of the behavior.
